echo "Running lint-staged"
pnpm exec lint-staged --config .lintstagedrc.format.mjs
pnpm exec lint-staged

echo "Scanning staged changes for secrets"
. "$(dirname "$0")/gitleaks.sh"
gitleaks git --redact --staged --verbose

# Agent Configuration

This repo's agent configuration lives in [`CLAUDE.md`](./CLAUDE.md).

Claude Code reads `CLAUDE.md` natively. If your agent reads `AGENTS.md` (Cursor, Windsurf, etc.), load rules from `CLAUDE.md` -- it is the single source of truth for this repo's conventions and working rules.
